{
  "name": "vamos-donde-salo",
  "version": "1.0.1",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "docker compose up",
    "prebuild": "npx pnpm install --no-frozen-lockfile --production=false",
    "build": "npx pnpm -r build",
    "start": "npx pnpm --filter @salo/web start",
    "start:web": "npx pnpm --filter @salo/web start",
    "start:api": "npx pnpm --filter @salo/api start",
    "seed": "docker compose exec api pnpm run seed",
    "db:migrate": "docker compose exec api pnpm run db:migrate",
    "db:reset": "docker compose exec api pnpm run db:reset",
    "db:studio": "docker compose exec api pnpm run db:studio",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
